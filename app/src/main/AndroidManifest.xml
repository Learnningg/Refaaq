<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alatheer.refaaq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.FamilyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme" />
        <activity android:name=".Activities.ProfileKafeelActivity" />
        <activity android:name=".Activities.ChooserActivity" />
        <activity android:name=".Activities.RegisterFamilyActivity" />
        <activity android:name=".Activities.RegisterDonerActivity" />
        <activity android:name=".Activities.RegisterPoorActivity" />
        <activity android:name=".Activities.RegisterKafeelActivity" />
        <activity android:name=".Activities.CallUsActivity" />
        <activity android:name=".Activities.ProgramsActivity" />
        <activity android:name=".Activities.AboutUsActivity" />
        <activity
            android:name=".Activities.Family_Activity"
            android:label="@string/title_activity_family_"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>